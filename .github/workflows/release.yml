name: release
on:
  push:
    tags:
      - 'v*'
permissions:
  contents: write
jobs:
  pack-zip:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: '1.23'
      - run: make verify
      - run: make pack-zip
